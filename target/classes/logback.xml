<configuration>
    <!-- Set the root logger level to ERROR to suppress INFO log messages -->
    <root level="INFO"/>

    <!-- Spark and Akka loggers will inherit the root logger's level -->
    <logger name="org.apache.spark" level="INFO" />
    <logger name="akka" level="INFO" />

    <!-- Other loggers and configurations can be added as needed -->

</configuration>